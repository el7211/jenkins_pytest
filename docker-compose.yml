services:
  pytest-runner:
    image: python:3.11
    container_name: test-runner
    volumes:
       - .:/app
    working_dir: /app
    command: >
      bash -c '
      echo "files in /app: " &&
      ls -la /app &&
      echo "files in /app/workspace: " &&
      ls -la /app/workspace &&
      echo '🚀 Running pytest...' &&
      pytest --tb=short -v | tee /dev/stdout'

#      pip install -r requirements.txt &&