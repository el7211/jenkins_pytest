services:
  pytest-runner:
    image: python:3.11
    container_name: test-runner
    volumes:
       - .:/app
    working_dir: /app/workspace
    command: >
      bash -c "
      cd jenkins_pytest &&
      pip install -r requirements.txt &&
      echo '🚀 Running pytest...' &&
      pytest --tb=short -v"

