services:
  pytest-runner:
    image: python:3.11
    container_name: test-runner
    volumes:
       - .:/app
    working_dir: /app
    command: >
      bash -c "
      echo '📂📂 Listing /app directory...' &&
      ls -la /app &&
      echo '📂📂 Listing /app/workspace directory...' &&
      ls -la /app/workspace &&
      tail -f /dev/null"

#      pip install -r requirements.txt &&